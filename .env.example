# Copy to .env and fill in values before running
DJANGO_SECRET_KEY=change-me-in-production
DEBUG=true

# Optional: get a free token at https://www.renewables.ninja
# If not set, seed_data falls back to a synthetic clear-sky model
NINJA_TOKEN=
